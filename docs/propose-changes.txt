-- What if user dont wanna clone it from somewhere? he has repo on other machine or cloud etc
-- Before cloning if we can fetch the repo details like branches etc, and ask the user about which branch to clone etc
-- User wanna make changes in the codebase then? from mcp it is doable but what about frontend? can we show the file structure? or something like that?
-- How to make the searching better and accurate
-- How to protect it from prompt injection?
-- It can generate sources files like instruction, rules, skill for the repo to download
-- Make sure there is pagination in the chat listing and message listing & the filteration should be at server side, Add search bar as well for chat findings
-- User can upload file or image snapshot and ask questions from it

-- NEW PROPOSED FEATURES:
-- Interactive Architecture Dependency Graph (Visualizer): Render a 2D map of the codebase for visual navigation.
-- Automated PR Review Mode: Analyze a Pull Request against the codebase to act as an AI reviewer.
-- Code Sandbox Execution: Run the codebase in a secure browser sandbox to test AI fixes.
-- "Explain this file" Annotations: Auto-generate summaries for files hovering in the frontend File Explorer.
-- Self-Healing Tests: Intercept failing tests and automatically suggest the fix in chat.
-- UI Revamp: Modernize the design with premium aesthetics, glassmorphism, and dynamic micro-animations to create a 'wow' factor.
-- Branched Chats (Tree Structure): Allow users to fork a conversation from any past message to explore multiple different directions.